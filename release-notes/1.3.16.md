# OnlyEQ 1.3.16

- The A/B control shows a dot under each slot that holds a curve, so an empty B is visible before the first switch copies A into it. The tooltip and VoiceOver say the same.
- Switching A/B is an undo step. Cmd-Z returns to the slot you left, with the curve it had.
