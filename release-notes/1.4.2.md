# OnlyEQ 1.4.2

- The line under the preset in the popover no longer says "Not saved for this device". When the preset on screen is not what the device is bound to, it reads "Use on <device> automatically" and one click binds it. A device bound to a preset that was deleted counts as unbound.
- A binding whose stored ID is stale but whose name still matches a preset counts as bound, as it already did when auto-applying.
