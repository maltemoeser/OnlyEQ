# OnlyEQ 1.3.19

- One name for the editing window: it is "Equalizer" in its title bar, on the popover button, in the right-click menu, and on the Settings back button.
- Onboarding copy in the app's own voice: "A parametric EQ for everything your Mac plays. Nothing to install, nothing to babysit." and "Pick your headphones to import a preset tuned for them." Buttons use title case like the rest of the app.
- The buffer-size note reads "Larger buffers are more stable but add latency."
- The "Double-click graph to add band" hint shows only while the preset has no bands.
