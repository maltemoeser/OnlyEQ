# OnlyEQ 1.3.18

- At 32 bands the add button greys out and the graph hint reads "32 bands maximum" instead of silently ignoring the click.
- Onboarding says when a preset fails to download and lets you press Start Listening again.
- The Browse tab has Retry buttons for a database that would not load and for a preset preview that failed.
- The drop zone lights up in the accent colour while a file is dragged over it.
- A search with no matches says so instead of showing an empty list, in both the import sheet and onboarding.
