# OnlyEQ 1.3.9

- The curve display widens in 6 dB steps when a preset has bands beyond ±12 dB, so imported AutoEq and peqdb bands are drawn where they are instead of pinned to the edge. The editor, popover, and import preview all use the same range, and typed gains accept up to ±30 dB.
