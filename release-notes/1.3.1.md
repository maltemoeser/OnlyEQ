# OnlyEQ 1.3.1

- The live spectrum now shows the untouched input in grey behind the processed output in blue, so you can see what the EQ is doing: boosts rise above the grey, cuts leave grey showing. The output is aligned for preamp and output gain, so only the filter shape shows as a difference.
