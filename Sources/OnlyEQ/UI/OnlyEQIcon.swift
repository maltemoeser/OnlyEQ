import Foundation

enum OnlyEQIcon {
    /// One mark across the menu bar, in-app branding, and generated app icon.
    static let symbolName = "chart.bar.fill"
    /// The same mark, outlined: shown in the menu bar while the EQ is off or
    /// bypassed, so the state reads without opening anything.
    static let inactiveSymbolName = "chart.bar"
}
